{
  "name": "figify-mcp-plugin",
  "version": "1.0.0",
  "description": "Figma plugin for figify-mcp - receives screenshots from MCP server and creates frames",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.90.0",
    "typescript": "^5.3.0"
  }
}
